-- 코드를 입력하세요
WITH BASE AS(

    SELECT * FROM JULY
    
    UNION ALL
    
    SELECT * FROM FIRST_HALF
)
, SUMMING AS(
    SELECT FLAVOR, SUM(TOTAL_ORDER) ADDED
    FROM BASE
    GROUP BY 1
    )
SELECT FLAVOR
FROM SUMMING
ORDER BY ADDED DESC
LIMIT 3
